//to get path of specific file
const path = require("path");
a = path.basename('c:\\temp\\myFile.html') //It is used to get the last part of a path, often referred to as the "basename."
console.log(a)